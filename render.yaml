services:
  - type: web
    name: cool-cruze-rental
    runtime: node
    buildCommand: cd client && npm install && npm run build && cd .. && npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
      value: 20
